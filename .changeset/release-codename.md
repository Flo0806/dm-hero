---
"@dm-hero/app": patch
---

feat: show a per-release codename in the header — each minor release gets its own name in its own font (1.4 = "The Summoning", Pirata One). Fonts are bundled locally (offline-safe).
